import React from 'react';

const UnauthorisedLayout = ({ children }) => {
  return <div className="flex justify-center items-center min-h-screen bg-[#fff]">{children}</div>;
};

export default UnauthorisedLayout;
